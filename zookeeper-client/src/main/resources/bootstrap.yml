spring:
  profiles:
    active: dev
  application:
    name: zookeeper-client
  cloud:
    zookeeper:
      connect-string: localhost:2181
      config:
        enabled: true
        root: config
        defaultContext: xjch
        #profileSeparator: -


